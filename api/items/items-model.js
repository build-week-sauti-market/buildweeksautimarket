//const db = require("../data/db-config")

function find(){
	
}

function findBy(filter){//eslint-disable-line

}

function findById(id){//eslint-disable-line

}
function findUserByName(username){//eslint-disable-line
	
}

function findItemByName(item_name){//eslint-disable-line

}

function findItemByLocation(location){//eslint-disable-line 
	
}

async function add(item){//eslint-disable-line 
	
}

function update(id, changes){//eslint-disable-line 
	
}

function remove(id){//eslint-disable-line 
	
}

module.exports = {
	find,
	findBy,
	findById,
	findUserByName,
	findItemByName,
	findItemByLocation,
	add,
	update,
	remove,
}